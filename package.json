{
  "name": "aligned-ai-benchmarks",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "broken-links": "mint broken-links",
    "generate:firegraph": "node scripts/generate-firegraph-svg.mjs"
  },
  "devDependencies": {
    "mintlify": "^4.2.631"
  }
}
