{
  "name": "aligned-ai-benchmarks",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "broken-links": "mint broken-links",
    "generate:firegraph": "node scripts/generate-firegraph-svg.mjs",
    "generate:cefeai": "node scripts/generate-cefeai-svg.mjs",
    "generate:assets": "npm run generate:firegraph && npm run generate:cefeai"
  },
  "devDependencies": {
    "mintlify": "^4.2.631"
  }
}
